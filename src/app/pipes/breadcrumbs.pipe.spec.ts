import { BreadcrumbsPipe } from './breadcrumbs.pipe';

describe('BreadcrumbsPipe', () => {
  it('create an instance', () => {
    const pipe = new BreadcrumbsPipe();
    expect(pipe).toBeTruthy();
  });
});
